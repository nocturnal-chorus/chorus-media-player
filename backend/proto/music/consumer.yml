type: google.api.Service
config_version: 3

http:
  rules:
    - selector: player.music.MusicService.ListMusic
      body: "*"
      post: "/v0/music"
    - selector: player.music.MusicService.GetMusic
      get: "/v0/music"
